name: CI/CD Pipeline

# Trigger the workflow on push or pull request to the main branch
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # ---------------------------------------------------------
  # JOB 1: TEST (Run Pytest)
  # ---------------------------------------------------------
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tests with Pytest
      env:
        POSTGRES_USER: test_user
        POSTGRES_PASSWORD: test_pass
        POSTGRES_HOST: localhost
        POSTGRES_PORT: 5432
        POSTGRES_DB: test_db
        REDIS_URL: redis://localhost:6379/0
      run: |
        export PYTHONPATH=$PYTHONPATH:.
        pytest -v

  # ---------------------------------------------------------
  # JOB 2: DEPLOY (SSH into Server & Upgrade)
  # ---------------------------------------------------------
  # deploy:
  #   # Only run this job if 'test' succeeded
  #   needs: test
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Deploy to Ubuntu Server via SSH
  #     uses: appleboy/ssh-action@v1.0.3
  #     with:
  #       host: ${{ secrets.SSH_HOST }}
  #       username: ${{ secrets.SSH_USER }}
  #       key: ${{ secrets.SSH_KEY }}
  #       # This script runs on your remote server
  #       script: |
  #         cd ${{ secrets.PROJECT_PATH }}
          
  #         # 1. Pull the latest code
  #         git pull origin main
          
  #         # 2. Rebuild and restart containers
  #         # --no-cache is important to pick up new Python packages (like trafilatura)
  #         docker-compose down
  #         docker-compose build --no-cache
  #         docker-compose up -d
          
  #         # 3. Run Database Migrations (CRITICAL)
  #         # The -T flag disables pseudo-TTY allocation, needed for SSH automation
  #         docker-compose exec -T worker alembic upgrade head
          
  #         echo "Deployment Successful!"