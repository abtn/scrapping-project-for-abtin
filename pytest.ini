[pytest]
# Ignore the unrelated qwen-code directory found in your environment
norecursedirs = qwen-code
# Explicitly tell pytest to look in our tests folder
testpaths = tests
# Optional: Add markers or default flags if needed later